{
    "name": "unminimize",
    "id": "unminimize",
    "version": "0.0.1",
    "description": "Install if not present unminimize and run it",
    "options": {
        "run": { 
        "type": "boolean",
        "default": "true",
        "description": "Select if unminimize should run"
        }
    }



}